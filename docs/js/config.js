// The one place your backend's URL is defined — api-status.js, dashboard.js,
// and ml-lab.js all read this instead of each hardcoding their own copy, so
// there's only one line to change if the API ever moves.
const API_BASE = "https://syoungcode-github-io.onrender.com";
