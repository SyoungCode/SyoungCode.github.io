# database/ — Phase 4

Empty for now. This holds:

- `schema.sql` — table definitions for the Olist e-commerce dataset
- `seed.py` — loads the Olist CSVs into Postgres (hosted on Supabase)
- `queries.sql` — the dashboard's SQL, kept readable outside the API code

Raw Olist CSVs are not committed here (see `.gitignore`) — keep them local or re-download
from Kaggle when seeding.
